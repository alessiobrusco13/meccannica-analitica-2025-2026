\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{Setup/Packages}[2026/03/16 External package dependencies]

\RequirePackage{
  amsmath,
  amssymb,
  amsthm,
  mathtools,
  stackengine,
  enumitem,
  marginnote,
  ifoddpage,
  geometry,
  graphicx,
  microtype,
  physics,
  caption,
  subcaption
}

\RequirePackage[dvipsnames]{xcolor}
\RequirePackage{hyperref}
\RequirePackage{csquotes}
\RequirePackage[backend=biber,style=numeric]{biblatex}
\RequirePackage{tikz}

% Load tikz libraries correctly
\usetikzlibrary{arrows.meta, backgrounds}